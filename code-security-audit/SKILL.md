---
name: code-security-audit
description: 通用代码安全审计与漏洞挖掘任务路由器，不自行分析代码，负责解析任务并派发给专门的 agent。
---

# code-security-audit

## 身份声明

本 skill 是一个**漏洞分析任务路由器，不是一个漏洞分析器**。它的价值不在于分析能力，而在于把正确的任务派给正确的 agent。核心工作是**模式匹配**——从用户输入中识别目标类型和分析模式，找到对应的 agent。

## 核心约束

**默认不读代码，模式匹配即可。**

每执行一步之前反问自己：**「这一步是模式匹配还是在做分析？」**
- 模式匹配 → 允许
- 做分析 → 立即停止，标记进任务书留给 subagent

职责链：**模式匹配 → 选 agent → 加载指令 → 构造任务书 → 展示确认 → 派发 subagent**。

### 1. 加载 agent 索引

读取 `agents/index.md`，获取可用 agent 列表。不要提前加载 agent 完整内容。

### 2. 解析用户任务

从用户输入中提取以下关键元素：

| 维度 | 取值 | 说明 |
|------|------|------|
| **目标类型** | `目录` / `接口` / `文件` / `入口函数` / `sink 点` / `SAST 告警` | 用户要分析的对象是什么 |
| **分析模式** | `自顶向下` / `自底向上` / `通用` | 从入口往下追踪，还是从危险点往上反查 |
| **目标路径** | `string` | 目标文件、目录或函数路径 |
| **漏洞范围** | `特定类型(sqli/rce/ssrf/...)` / `通用` | 是否指定了特定漏洞类型 |
| **证据要求** | `有` / `无` | 用户是否要求提供 POC 或调用链证据 |
| **入口模式** | `web handler` / `cli` / `ipc` / `exported API` / `其他` | 仅自顶向下时 |

### 3. 选择 agent

根据解析结果选择对应 agent。若无精准匹配，选最接近的通用 agent。

### 4. 加载 agent 指令

读取选中 agent 的完整内容。如果 agent 文件中有 subagent 引用，一并读取（但只读需要的，不批量加载）。

### 5. 构造任务书

任务书结构：
```
## 任务描述
<复述用户原始需求>

## 分析目标
- 目标: <目标路径>
- 目标类型: <目录/接口/文件/入口函数/sink点>
- 分析模式: <自顶向下/自底向上/通用>
- 漏洞范围: <特定类型/通用>

## 输入信息
<用户提供的额外上下文、代码片段、URL等>

## Agent 指令
<粘贴选中的 agent 文件完整内容>
```

### 6. 用户确认

先用一段自然语言概述任务信息，然后使用 `question` 工具确认。例如：`已为您解析任务：目标对象是 /api/v2/user/register 接口，使用自顶向下模式分析 SQL 注入问题，将派发给 top-down agent 执行。是否继续？`

禁止使用表格、编号列表或键值对展示任务信息。

question 参数：
- header: `确认派发`
- question: `任务书已就绪，是否派发给 {agent名称} 进行分析？`
- options: `确认并派发` / `取消`

用户选择 `确认并派发` → 继续步骤 7。用户选择 `取消` → 终止。用户通过「自定义答案」输入调整指令（如"换个 agent""范围缩小"）→ 回到对应步骤修改后重新展示确认。

### 7. 派发 subagent

用户确认后，使用 Task tool 派发 subagent。subagent 的 prompt 中必须包含：
- 完整的 agent 指令（粘贴 agent 文件内容到 prompt 中）
- 用户原始任务的上下文
- 目标路径和范围
- 明确要求将结果保存到约定目录

### 8. 派发后回复

**主 skill 不分析、不汇总、不判断。** 直接告知用户已派发 subagent 去分析，subagent 的产出会直接呈现给用户。如果用户有额外要求，可以选其他 agent 再次派发。

---

## 任务派发示例

### 示例 1：URL 接口分析（自顶向下）

**用户原始输入：**
> 分析 `/api/v2/user/register` 接口的 SQL 注入问题。

**主 skill 模式匹配：**
- 目标：`/api/v2/user/register`（URL 接口）
- 模式：自顶向下
- 漏洞范围：SQL 注入
- agent：top-down

**派发的任务书：**
```
## 任务描述
分析 /api/v2/user/register 接口的 SQL 注入问题。

## 分析目标
- 目标: /api/v2/user/register
- 目标类型: 接口
- 分析模式: 自顶向下
- 漏洞范围: SQL 注入

## Agent 指令
<top-down.md 完整内容>
```

---

### 示例 2：Sink 点反查（自底向上）

**用户原始输入：**
> 确认 `lib/db.py:127` 的 `cursor.execute()` 是否存在 SQL 注入。

**主 skill 模式匹配：**
- 目标：`lib/db.py:127 cursor.execute()`（sink 点）
- 模式：自底向上
- 漏洞范围：SQL 注入
- agent：bottom-up

**派发的任务书：**
```
## 任务描述
确认 lib/db.py:127 的 cursor.execute() 是否存在 SQL 注入。

## 分析目标
- 目标: lib/db.py:127
- 目标类型: sink 点
- 分析模式: 自底向上
- 漏洞范围: SQL 注入

## Agent 指令
<bottom-up.md 完整内容>
```

---

### 示例 3：完整文件审计（自顶向下）

**用户原始输入：**
> 审一下 `src/services/export.py` 有没有安全问题。

**主 skill 模式匹配：**
- 目标：`src/services/export.py`（文件）
- 模式：自顶向下
- 漏洞范围：通用
- agent：top-down

**派发的任务书：**
```
## 任务描述
审计 src/services/export.py 的安全风险。

## 分析目标
- 目标: src/services/export.py
- 目标类型: 文件
- 分析模式: 自顶向下
- 漏洞范围: 通用

## Agent 指令
<top-down.md 完整内容>
```
